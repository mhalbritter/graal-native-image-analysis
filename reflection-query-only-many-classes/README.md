# reflection-query-only-many-classes

Contains 100 classes with 100 method each, which are only queried
through `getDeclaredConstructors` and `getDeclaredMethods` and are **not** invoked via
reflection.
